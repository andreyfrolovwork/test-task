const key = '2'

const tables = {
    passport:'passport' + key,
    child:'child' + key,
    client:'client' + key,
    job:'job' + key,
    address:'address' + key,
    testAddress:'testAdress' + key,
    communication:'communication' + key
}

export default tables