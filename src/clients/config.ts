const key = '006'

const tables = {
    passport:'passport' + key,
    child:'child' + key,
    client:'client' + key,
    job:'job' + key,
    address:'address' + key,
    testAddress:'testAdress' + key
}

export default tables